import { FormatoTituloPipe } from './formato-titulo.pipe';

describe('FormatoTituloPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoTituloPipe();
    expect(pipe).toBeTruthy();
  });
});
