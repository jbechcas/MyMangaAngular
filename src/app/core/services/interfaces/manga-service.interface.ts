import { Manga } from '../../models/manga.model';
import { IBaseService } from './base-service.interface'; 

export interface IMangaService extends IBaseService<Manga> {
    // Métodos específicos si se necesitan
}